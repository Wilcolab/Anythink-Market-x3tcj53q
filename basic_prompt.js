/**
 * Converts a string to camelCase format.
 * 
 * Takes a string with words separated by spaces, hyphens, or underscores
 * and converts it to camelCase (first word lowercase, subsequent words capitalized, no separators).
 * 
 * @param {string} str - The input string to convert
 * @returns {string} The camelCase version of the input string
 * 
 * @example
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("convert-this-string") // returns "convertThisString"
 * toCamelCase("snake_case_example") // returns "snakeCaseExample"
 */
function toCamelCase(str) {
    // TODO: Implement the function
}

module.exports = toCamelCase;